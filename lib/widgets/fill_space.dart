import 'package:flutter/material.dart';

// using for distance between widgets
class FillSpace extends StatelessWidget {
  const FillSpace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(),
    );
  }
}
